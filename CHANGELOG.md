# 更新日志

> 发版流程：在此文件顶部新增一节 `## <版本号>`（与即将打的 `v<版本号>` tag 对应），
> 写本次更新内容；CI（`.github/workflows/release.yml`）按 tag 自动提取对应小节注入 GitHub Release，
> App「设置 › 关于」在检测到新版本时展示这段内容。版本号与 `MARKETING_VERSION` 保持一致。

## 1.2.0

- 基于订阅规则能力升级到 sing-box 1.14.0-alpha.39，作为 1.2.0 测试分支发布。
- 调整 1.14 TUN DNS 接管方式：TUN DNS 查询通过 helper 转发到内核 DNS 服务，避免 macOS 系统 DNS 指向本地 TUN 地址后无法稳定解析。
- 按内核版本同步 App 内置 sing-box 二进制，避免关于页版本、打包资源和实际运行内核不一致。
- 延续 TUN/helper 异常恢复和 DNS 引用规范化修复，确保 helper 失联、启动失败或 Mixin 引用 `system-dns` 时不会留下不可用 DNS 配置。

## 1.1.22

- 新增本地订阅文件导入：可直接选择本机 Clash / Stash、sing-box JSON、base64 或分享链接列表文件，Sail 会保存文件访问权限并在刷新时重新读取；本地订阅默认关闭自动更新，避免把本地文件误当远程订阅轮询。
- 增强 Clash / Stash 订阅兼容性：支持从订阅中导入原始 `proxy-groups`，在分组页保留机场自带的 selector / url-test 结构；支持 `use`、`include-all`、`filter`、`exclude-filter` 展开节点池，并处理 `DIRECT` / 拦截类特殊出站。
- 规则导入仍要求 Clash / Stash 配置包含 `rules`：仅有 `proxy-groups` 的配置不会单独导入分组，避免 groups-only 改变现有导入语义；刷新非 Clash 内容或关闭导入时不会误用陈旧的订阅规则缓存。
- 改进订阅分组的空过滤器处理：当地区 filter 没有匹配到节点时，不再静默把该组改成直连；会跳过空组、级联清理父组引用，并过滤掉指向不存在出站的订阅规则，避免生成无效配置或在用户不知情时走直连。
- 改进订阅规则集本地缓存：`route.json` 里的本地规则集路径改为记录同目录相对文件名，运行时再按正式 `subrules/<订阅ID>/` 目录解析，避免临时目录 `.tmp-*` 路径残留导致内核启动时报找不到规则集文件。
- 新增 sing-box 运行设置：可在设置页调整内核日志级别、远程 DNS、直连 DNS、引导 DNS、指定域名 DNS、延迟检测 URL 和自动测速间隔；DNS 相关修改加入防抖，连续调整时只触发一次内核重启。
- 修复 TUN + 直连模式下 DNS 劫持的 fallback：直连模式现在稳定使用直连 DNS，不会因为存在代理节点而兜底到远程 DNS。
- 日志页过滤语义改为精确匹配所选级别：选择“错误”只看错误日志，选择“信息”只看信息日志，不再显示所选级别及更高等级的混合集合。
- 设置页输入体验优化：端口、延迟检测 URL、域名 DNS 等表单改为提交或失焦后校验；点击空白区域会释放输入焦点，新增空白域名 DNS 草稿不会立即标红。
- 连接页顶部布局优化：统计、搜索、排序和“断开全部”操作保持单行三栏排列；“断开全部”明确表示关闭当前活动连接，并避免窗口较窄时按钮被压缩成省略号。
- 分组测速更准确：嵌套分组显示其叶子节点的聚合延迟，真实节点延迟更新不会覆盖嵌套组的聚合结果。
- 修复多处构建和静态分析警告：处理 Helper `fcntl` 返回值，补齐 Swift 并发隔离标注，替换弃用文件选择 API，并修复 Xcode App Intents 元数据相关 warning。

## 1.1.21

- 修复规则模式下访问国内网站（百度、抖音等）虽走直连却很慢的问题：根因是 TUN 模式下所有 DNS 查询都兜底走了经代理的海外 DNS，把国内域名解析成了海外 CDN 节点，于是直连也连到了绕远路的海外 IP。现让国内域名（geosite-cn）的 DNS 解析走国内 DNS（223.5.5.5）直连，拿到就近 CDN，直连即就近、不再慢。

## 1.1.20

- 彻底修复关闭 TUN 后内核仍反复重启、日志刷 `address already in use` 的问题：根因是上一个内核在 7890 端口上的连接残留 TIME_WAIT、挡住新内核绑定。现让 App 自身经代理的请求（查出口 IP / 下载 / 更新订阅）用完即主动关闭连接，不在 7890 留下 TIME_WAIT；并把内核启停串行化，快速开关 TUN 也不会再有两个内核抢同一端口。
- 概览页出口 IP 信息不再每次进入页面就自动刷新：改为仅在开关系统代理 / TUN 后延迟约 5 秒自动刷新一次（连续开关只刷最后一次），平时手动刷新即可。
- 更新窗口的更新日志改为列表条目渲染，不再是挤在一起的纯文本破折号。

## 1.1.19

- 修复关闭 TUN 后内核仍反复重启、日志刷 `address already in use` 的问题：切换时未等旧内核真正释放端口就启动新内核，现改为确认混合端口空出后再启动，无论旧内核退出快慢都不会再撞端口。
- 加固 TUN 特权组件：停止内核时按可执行路径清除可能遗留的内核进程（覆盖特权组件重装 / 重启遗留的残留）。
- 注：本版升级了 TUN 特权组件，已启用 TUN 的用户更新后首次启动会弹一次管理员授权以同步组件，属正常现象。

## 1.1.18

- 修复关闭 TUN（虚拟网卡）模式时内核反复重启、日志刷 `address already in use`（并发启动把同一端口双开占用）。
- 修复导入个别格式异常的订阅（含超大端口字段）时 App 崩溃。
- 修复检查更新 / 关闭更新窗口后 App 偶发崩溃。
- 修复节点数量很多的大型订阅在 TUN 模式下可能启动失败。
- 修复下载更新时关闭主窗口会连带隐藏更新窗口、以及关闭更新窗口影响主窗口。
- 加固 TUN 特权 helper：收紧文件描述符继承、缩小内核启动竞态窗口。
- 系统代理设置失败时记录诊断日志，不再静默吞掉。
- 注：本版升级了 TUN 特权组件，已启用 TUN 的用户更新后首次启动会弹一次管理员授权以同步组件，属正常现象。

## 1.1.17

- 修复内核偶发异常退出（code 1）：关闭代理 / 切换开关时，clash_api 端口与上一内核实例残留的 TIME_WAIT 状态冲突（`address already in use`）。现改为每次启动选用新的空闲端口，并避开其它 Clash 客户端固定占用的 9090。

## 1.1.16

- 内核改为随版本内置分发，移除 App 内联网下载/更新入口（发新版即更新内核）。
- 修复 TUN 模式下每次启动都弹管理员密码：helper 版本检查改为自愈，杜绝版本号两处漂移。
- 设置页移除「内核」分区，内核版本移至「关于」页展示。
- 内置 sing-box 升级至 1.13.14。
- 修复 macOS 15 上添加 / 编辑订阅时，长订阅链接在输入框被截断。
- 日志页：手动上滚查看历史时不再被持续输出强制拽回底部，并提供「回到底部」按钮。
- 检测到新版本时，侧栏图标显示 NEW 角标，点击打开更新窗口。
- 更新改为独立窗口：展示本版更新日志 + 取消 / 更新，点「更新」在窗口底部就地显示下载 / 安装进度（「设置 › 关于」的「查看更新」同此窗口）。
